# Example environment configuration for the survey platform

# PostgreSQL connection string (replace with your credentials)
DATABASE_URL=postgresql://user:password@localhost:5432/survey_db

# Secret used for signing cookies (generate a random string)
COOKIE_SECRET=change-me

# Admin credentials (in a real application, use a proper auth system)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=supersecurepassword